import React from 'react';
import '/Main.css';
import backgroundImg from '../background.png';

export default function Main({ animals }) {
    return (
        <main style={{ backgroundImage: `url(${backgoundImg})` }}>

        {animals.map((animals, i) => <)}

        </main>
    )


}